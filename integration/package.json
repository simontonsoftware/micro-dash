{
  "name": "integration-test",
  "version": "1.0.0",
  "description": "App for integration tests",
  "scripts": {
    "clean": "rimraf out-tsc/* dist/*",
    "prebuild": "yarn install --check-files && yarn clean",
    "build": "yarn build:src && yarn build:e2e && yarn build:aot",
    "build:src": "tsc -p src/",
    "build:watch": "yarn build:src -- -w",
    "build:e2e": "tsc -p e2e/",
    "build:aot": "node build.js",
    "serve": "lite-server -c=bs-config.json",
    "serve:aot": "lite-server -c bs-config.aot.json",
    "serve:e2e": "lite-server -c=bs-config.e2e.json",
    "serve:e2e-aot": "lite-server -c bs-config.e2e-aot.json",
    "start": "concurrently \"yarn build:watch\" \"yarn serve\"",
    "pree2e": "yarn build",
    "e2e": "concurrently \"yarn serve:e2e\" \"yarn protractor\" --kill-others --success first",
    "pree2e:aot": "yarn build",
    "e2e:aot": "concurrently \"yarn serve:e2e-aot\" \"yarn protractor\" --kill-others --success first",
    "preprotractor": "webdriver-manager update",
    "protractor": "protractor protractor.config.js"
  },
  "author": "Simonton Software",
  "license": "MIT",
  "dependencies": {
    "core-js": "^2.5.1",
    "systemjs": "0.19.40"
  },
  "devDependencies": {
    "@angular/compiler": "^5.0.3",
    "@angular/compiler-cli": "^5.0.3",
    "@angular/core": "^5.0.3",
    "@types/jasmine": "^2.8.2",
    "concurrently": "^3.5.1",
    "glob": "^7.1.1",
    "jasmine-core": "^2.8.0",
    "lite-server": "^2.2.2",
    "protractor": "^5.2.0",
    "rimraf": "^2.6.2",
    "rollup": "^0.42.0",
    "rollup-plugin-commonjs": "^8.0.2",
    "rollup-plugin-node-resolve": "3.0.0",
    "rollup-plugin-uglify": "^2.0.1",
    "rxjs": "^5.5.2",
    "typescript": "^2.6.1"
  }
}
